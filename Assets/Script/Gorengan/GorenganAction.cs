using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GorenganAction : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	//void FixedUpdate () {
 //       CheckCookingStatus();
 //   }

    //void CheckCookingStatus()
    //{
    //    if (GameManager.Instance.cookingStatus == true)
    //    {
    //        if(GameManager.Instance.ChosenGorengan != null)
    //            this.name = GameManager.Instance.ChosenGorengan.ids;
    //        GameManager.Instance.cookingStatus = false;
    //    }
    //}
}
